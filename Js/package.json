{
  "name": "js",
  "version": "1.0.0",
  "scripts": {
    "debug-build": "npm run pack",
    "pack": "set NODE_ENV=development&&node pack/pack.js",
    "build": "set NODE_ENV=production&&node pack/pack.js",
    "tsbuild": "tsc -p tsconfig.json",
    "npm-p": "npm install --production"
  },
  "dependencies": {
    "react": "^17.0.1",
    "react-reconciler": "^0.26.1"
  },
  "devDependencies": {
    "@babel/core": "^7.12.10",
    "@babel/preset-env": "^7.12.11",
    "@types/react": "^17.0.0",
    "@types/react-reconciler": "^0.18.0",
    "typescript": "^4.1.3"
  }
}
